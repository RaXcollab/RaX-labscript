# Open Items — cross-session ledger

Deferred work with an owner and a gate. A Claude session that flags something as "worth a follow-up" MUST add it here (or fix it) — prose footnotes don't survive compaction. Remove items when done.

## Gated on operator / hardware
- **Camera-fix hardware validation** (updated 2026-07-08): morning merges VERIFIED (19:46 15-shot queue clean). Remaining: BLACS restart to activate the overnight hardening (b75b98b + labscript-devices b32c97e) — checklist in `notes/2026-07-08_Nuvu-error27-hardening-and-teardown-fixes.html`; then one slow-lock shot + manual snap.
- **Hooks v2 settings-dispatch test** (plan T2 step 5, narrowed 2026-07-08): scripts smoke-tested 24/24 (both PS editions, byte-exact stdin) — remaining untested surface is Claude Code's settings-level dispatch only. Fresh session → checklist appears (also on `--resume`); scratch edit under `userlib/user_devices/` → audit gate fires once; chained `git add x && git commit` via Bash → blocked by git guard. Note: creating the audit-waiver marker will hit a permission prompt (allow-rule was classifier-denied; waiver now requires user click — keep it that way unless it annoys).

## Code follow-ups (NuvuCamera, pre-existing, audit-flagged 2026-07-08)
- ~~`errorHandling` 107 dead `pass`~~ FIXED in working tree by concurrent session (verified [nc_camera.py:109-116](userlib/user_devices/NuvuCamera/Nuvu_sdk/nc_camera.py#L109-L116) — 107 now returns without closing; 215/216 also reclassified no-close). Committed in `ba5bf63` (verified 2026-08-04: NuvuCamera tree clean, `git log -S` pins both fixes there).
- ~~`get_bias` exposure leak~~ RESOLVED — get_bias/get_bias64 deleted 2026-07-08 (no callers; verified [Nuvu_cam_utils.py:217](userlib/user_devices/NuvuCamera/Nuvu_sdk/Nuvu_cam_utils.py#L217)). Committed in `ba5bf63` (verified 2026-08-04: NuvuCamera tree clean, `git log -S` pins both fixes there).
- ~~Abort-during-continuous double-resume~~ FIXED in b75b98b (`should_resume_continuous` guard + idempotent `start_continuous`; cross-audited, 31/31 tests). All three audit-flagged NuvuCamera items now closed.

## Context follow-ups
- ~~Digest refresh~~ DONE 2026-07-08 (3 opus agents): `cameras-and-scope.md`, `backend-core.md`, `labscriptlib.md` all stamped "Refreshed 2026-07-08" and verified against post-merge code + working tree.
- Always-loaded budget: 11,713 → **7,996 tokens** after 2026-07-08 no-deferrals restructure (under 8k target; history in `.claude/agent-memory/context-auditor/audit-history.md`). Re-measure at next T8 run.
- `Open_cell2.py`: unused `latch_digital` import (post-rework leftover, flagged by digest refresh) — one-line cleanup at next sequence edit.
- **TiSa_1 channel-move merge gate** (added 2026-07-29): TiSa_1 is now WS7 **ch1** (was ch4; crosstalk). The `zmq-v2-cutover` branch still carries `connection=4` in `connection_table.py` (both TiSa_1 children) and double-stale HF docs; `HF_Locking-zmq-v2` has NO per-port tolerance mechanism yet claims one in its CLAUDE.md. Fix at rebase/merge or the move regresses. Full checklist: `docs/wavemeter-channel-move.md`. Physical steps (PID DAC cable output 4→1, WS7 per-channel PID migration) may still be pending — confirm with operator. **Git half CLOSED 2026-08-04:** `zmq-v2-cutover` is fully merged into master (0 unmerged commits, master 14 ahead; TiSa_1 `connection=1` on BOTH branch and master) and HF `zmq-v2-port` is fully merged into HF main (0 unmerged) — no rebase/merge remains that could regress the move. Remaining: operator confirmation of physical steps; stale fully-merged branches/worktrees (`zmq-v2-cutover`, HF `zmq-v2-port`) are removal candidates pending user OK.
- **Graph rebuild gate** (re-armed 2026-08-12): rebuild `graphify-out/graph.json` after the next large cross-root refactor. Recipe + 14-check verify: `.claude/graphify/REFRESH.md`. Last rebuild 2026-08-12 post raster-arming + GUI-slowdown merges (commit 5e960af): 5729n/9373e/414 communities, 34 cross-repo edges incl. the GUIs→userlib bridge via `external_gui_lib/zmq_v2.py`; 14/14 verify checks passed (new check 1b: `GUIs/rastering-*` worktree duplicates filtered — first pass silently included 4 worktrees = ~2,750 duplicate nodes). Standing convention: re-arm after each rebuild rather than deleting.

- **PR#6 pad-line move** (2026-08-03): committed 04e9012 on `ct-enabled-switchboard` — `PARITY_PAD_LINE` now 6361 `port0/line7`, 6535 `port3/line7` (DIO 31), reserved-line docs in branch CLAUDE.md + `.claude/rules/sequences.md`. Remaining: runtime DAQmx acceptance of the port3 pad rides the PR's existing post-merge gate (BLACS restart + test shot with 6535 on) — remove this line at merge.

- **Rastering edge-coordinate hardening, deferred remainder** (2026-08-04, agent-audited twice; DONE: fresh-partner + wrap `9608d72`, motor_bounds(0-12mm)+units guard+10um edge clamp `6baf4fb`, courtesy-write `f9be922`). **Batch 2026-08-04 (evening): items (2),(3),(5),(6) + (1)-subparts IMPLEMENTED on topic branches — adversarially reviewed, suites green (parent 123 per-directory, rastering 120). MERGED 2026-08-05 (master `1d3ef43`, rastering main ff→`e538aa0`; blacs-expert audit SHIP-WITH-NOTES) + coordinated GUI+BLACS restart DONE. Log triage 2026-08-05 (19:06 restart, 55-shot run): CLEAN — 0 errors/tracebacks, faulthandler empty, no queue stalls, LaserLock T2B 5.6–11.7 ms; the one 12 s inter-sequence gap was runmanager compile, not poll pile-up. **Compound-write path CONFIRMED firing at runtime** (2026-08-05 19:35–19:36, log evidence): operator spinbox edits produced 4× `program_manual(laser_raster_xy=[…]) refused by the rastering GUI; continuing` WARNINGs (values 0.6/5.0/24.999/25.0 px — all map below motor travel via the calibration) — courtesy policy held: warn-and-continue, no red tab, no queue impact. A SUCCESSFUL pair move is UNVERIFIABLE from the BLACS panel until the units pass lands (every typeable value ≤25 px is out of travel — circular with the CT clamp). Remaining runtime check: (c) queue abort with GUI up → no red tab. h5 provenance VERIFIED from artifact: `data/RasteringGUI/raster` attrs on 2026-08-05_0004_10 carry `frame='pixel'` + calibration_matrix/offset + point_index/path_len. Minor operator nits pending list.**
  - (1) **Units pass (user-decided design, refined 2026-08-04):** show BOTH motor and pixel coords, pixel = N/A uncalibrated. **Frame principle: motor units are FIXED (0-12 mm), pixel units vary per calibration re-fit — never tie connection-table bounds to a specific calibration** (a pixel limit like "(0,5000)" is derived from the 2026-07-20 fit; rejected). **Target design: frame-explicit BLACS↔GUI communication — wire supports pixel OR motor coordinates (frame-tagged); once motor-frame comms exist, CT channels can declare motor units with fixed (0,12) limits.** Current CT `(0,25) mm` mislabel is a LABEL defect, **not** a compile blocker (corrected 2026-08-04): all three sequences call `Raster_X.constant(RASTER_X)` and the globals are `RASTER_X = 2.12667` / `RASTER_Y = 1.07629` (h5-verified), inside `(0,25)` *and* the target `(0,12)` — uncommenting compiles today. The `limits`→`LabscriptError` path is real but unarmed; only a pixel-scaled value (e.g. `110`) would trip it. Design doc: `docs/frame-explicit-coords-design.md`. Include in the pass: stamp frame/calibration on h5 `/data/{dev}` monitor_values (uncalibrated mm vs calibrated px currently indistinguishable); one-line latent motor-fallback removals at `raster_controller.py` CHECK_VALUE `:606/:608` + publisher `:2285`; `raster_point_meta` should emit explicit `frame:` key; clamp applies only to calibrated path (asymmetry OK but document). Protocol design gap: v2 HELLO has no per-connection units/frame declaration — the natural home for the frame tag. **PROGRESS 2026-08-04:** subparts landed on `feat/open-items-batch` — motor-fallbacks removed (`:606/:608`, `:2285`), `raster_point_meta` emits `frame:`, dual-frame readout mm-always/px-when-calibrated (`ef7c0b8`); parent `05a3acd` adds `frame` to `RASTER_META_KEYS` so it reaches shot h5 (review blocker, fixed). Design doc DRAFTED, uncommitted, awaiting user review: `docs/frame-explicit-coords-design.md` (261 lines, adversarially reviewed + revised against 12 verified findings; 'Open questions for the user' at end — incl. frame-token naming pixel-vs-target, invented `calibration_id` mechanism, RASTER_X/Y globals must become mm at migration). Remaining: monitor_values frame/calibration stamping is design-gated (PUB-SUB cache carries no frame today — doc §d); BLACS-side `frame` passthrough. **PROMOTED to operator-blocking 2026-08-05 (post-restart observation):** the CT mislabel functionally blocks all BLACS-panel moves — all four Raster channels declare `units="mm", limits=(0,25)` (connection_table.py:106-142) while carrying camera-pixel values (real position today x≈305 px, y≈204 px; usable px ≈110–4414), so the spinbox clamps every typed X to 25 px → maps through the calibration to motor x≈−0.24 mm → correctly refused (courtesy warn, no motion, no red tab). Monitors likewise show px under an mm label. NOT a compound-write defect — pre-batch single-axis writes refused identically; the pair form only made it visible. Panel coord boxes are effectively display-only echoes until the units pass lands; command moves from the rastering GUI.
  - (2) **Atomic (x,y) write — verified CONTAINED, not a refactor** (blacs-expert 2026-08-04): ~30-40 LOC, 2 files. Worker-level interception: `RasteringWorker.program_manual`/`transition_to_buffered` already hold both values (panel dict / one h5 row) → one compound PROGRAM_VALUE (e.g. `laser_raster_xy`) → GUI executes with EXISTING `request_move_target`/MOVE_TARGET (already clamps + bounds-checks the true pair). No CT/tab/widget/PUB/read-side changes. Gotchas: one-column h5 table (sequence sets only x) keeps single-axis path — NEVER fill partner from stale panel; compound failure must route through `_on_program_manual_error` or courtesy-write policy silently dies. Bonus: deletes the physical `(x_new, y_old)` intermediate excursion (only failure mode the clamp can't fix) + halves abort-path round trips. **IMPLEMENTED 2026-08-04**: parent `e517c86` (worker interception, gotchas honored, 6 new tests) + rastering `202004e` (compound handler w/ optional `frame` arg: `pixel` default, `motor`→MOVE_MOTOR, typed `invalid_frame`/`invalid_value`). Cross-side wire contract adversarially verified. **DEPLOY COUPLING: merge both + restart GUI and BLACS together — new BLACS against old GUI fails every both-axis shot with `unknown_connection` (manual writes only warn).** Residual notes (review + blacs-expert audit 2026-08-05, verdict SHIP-WITH-NOTES): **`program_value` args passthrough re-scoped from cosmetic to SHOT-FAILURE risk** — BLACS can't send `timeout_sec` (client hardcodes `args={"wait_for_lock"}`), so the GUI's internal 10 s cap binds while MOVE_TARGET moves x THEN y sequentially against ONE reply budget; a pair move with `10 s < t_x+t_y ≤ 20 s` in T2B → `motor_move_failed` → strict raise → **shot fails, sticky error, queue stalls** (bites a large jump, e.g. first shot from a parked position; typical raster steps are sub-second; `program_manual` side is harmless — courtesy warn while the motion completes). Fix = args-passthrough kwarg on the shared `program_value` (same change unlocks the frame tag / motor-frame writes — see design doc). Also: `RasteringWorker.program_manual` override duplicates the base loop verbatim (mirror future base-loop changes); compound connection-name literal not test-pinned across repos.
  - (3) **`MODE_POST_EXP` poll mask** — verified: blind window is inter-shot bookkeeping only (poll runs through MODE_BUFFERED; out-of-mode tick DROPPED not deferred, timer re-arms → worst case one missed 5s tick). Fix = add `|MODE_POST_EXP` to `RemoteControl/blacs_tabs.py:363-366` mask (fork's own `device_base_class.py:456` base already includes it — omission reads as oversight). Cost: REQ-REPs in inter-shot gap; hung GUI could delay next T2B ≤5s. UNBOUNDED exception: device in shot N but absent from later queued shots strands in POST_EXP (poll + program_device frozen) until pause — latent, rastering is in every shot. Fresher echo ≠ safer abort replay; decide on merits. **IMPLEMENTED 2026-08-04** `d66c782` on `open-items-batch` (tradeoff documented in commit body). Audit corrections (blacs-expert 2026-08-05): hung-GUI worst case is **N×5 s per device**, not ≤5 s — the poll loops every output channel and short-circuits only on a hard `None` (Rastering 2→10 s, LaserLock 3→15 s, BigSky 9→45 s; queue `timeout_limit=300` so the gap stretches, queue survives). Drop the commit at merge if that exposure is unacceptable. Stranded-tab escape is PARTIAL: the mask un-freezes the echo only — `program_device` stays MODE_MANUAL-only, operator edits still queue until pause. `status_monitor` (blacs_tabs.py:392-402) is DEAD CODE (zero call sites, audit-verified) — its old mask is moot. Pre-existing noise, newly extended into the gap — CORRECTED by 2026-08-05 log triage (the audit's "3 params → `unknown_monitor_param`" prediction had 0 log occurrences): the real noise is a laser that is NOT launched in the BigSky GUI answering `UNKNOWN_CONNECTION` for ALL its params → typed skip WARNINGs. Distribution analysis 2026-08-05: the flood is a PRE-EXISTING MANUAL-mode poll behavior (~72/min at idle, forever — 3546 by 19:41; "1112" was a mid-flood snapshot); the new POST_EXP mask contributed exactly 6 lines (0.17%, one ~27 ms inter-shot window hit in 55 shots). Mitigation menu → dedicated bullet below. Runtime verify at next BLACS restart: queue 3+ shots, compare PERF T2B timestamps.
  - ~~(4) Clamp ceiling~~ RE-CHECKED 2026-08-11 vs the new fit (rastering `63e1655`) — verdict OK, standing per-re-fit chore RETIRED. Structural: arm-filter vs step-gate disagreement is closed by construction (both call `_target_to_motor_clamped`, raster_controller.py:1933), so a re-fit can only (a) resize the dead zone — arm filter drops + names it (new fit: ~8 px left + ~49 px bottom strip, vs 22.7% under the old fit) — or (b) re-open single-axis edge-replay rejections when a measured-at-bound coordinate replays after partner drift > clamp/cross-term (new fit: >124 px y-drift for x edges, >76 px x-drift for y edges). Consequence bounded + loud: named rejection, courtesy warn on manual path (no queue stall); buffered fails a shot only if a sequence programs a coordinate AT a travel bound. If it ever bites: derive tol from the cal at load (~3 lines) — deliberately NOT done, a ~65 µm silent snap is worse than a loud rejection.
  - ~~(5) BLACS-tab `raster_progress` shows e.g. "37/12" after wrap~~ FIXED 2026-08-04 on `feat/open-items-batch` (`5fede41`+`d84a735`): `_raster_step_count` field deleted (a reset-on-wrap would race the queued Qt increment slot); progress text now derived wrap-sane at publish time.
  - (6) Dead code: ~~`request_jog_target`~~ deleted `e538aa0` (no callers, repo-wide grep); BigSky cosmetic substrings converted to typed checks in `75a41a3`. Leftover: orphaned `CommandType.JOG_TARGET` enum + `_execute` branch (only a test constructs it directly) — remove at next controller edit.

- **BigSky buffered-path typed conversion** (2026-08-04, from the program_manual consolidation audit — the manual path is DONE, hook + typed gates, 60/60 tests): `should_skip_buffered_response` (`BigSkyHub/blacs_workers.py:42`) still relies on `_SKIP_ERROR_SUBSTRINGS` — **load-bearing**: an offline laser arrives as `status=ERROR` + `code=laser_disconnected` and only the "laser disconnected" substring catches it (guard comment added at the tuple). Convert to typed `error.code` checks (add `laser_disconnected`), then retire the substrings. Also cosmetic-only substring sites at `_arm_laser` (~:189, greps `str(exc)`) and `_verify_armed_state` (~:547) — log-level only, both branches act identically. **DONE 2026-08-04** `75a41a3` on `open-items-batch`: buffered path on typed `error.code` gates (each substring mapped to its typed carrier and verified against HugeSkyController.pyw emissions BEFORE retiring; cosmetic sites converted; 19/19 tests incl. skip-on-`laser_disconnected` and raise-on-unknown-code). Review: pass-with-notes — two notes promoted to items below.
- **BigSky buffered REJECTED-any-code tolerance** (pre-existing, surfaced by 2026-08-04 review — entrenched, not introduced, by `75a41a3`): the buffered path skips on `status=REJECTED` regardless of `error.code`, so an h5-programmed refusal (e.g. `voltage_out_of_range`, interlock) silently skips instead of failing the shot (`BigSkyHub/blacs_workers.py:46`, skip at `:490-494` precedes `_check_response`). Decide: narrow to specific codes, or document as intended queue-liveness behavior.
- ~~**Pre-push hook TEST_PATHS gap**~~ **RESOLVED 2026-08-12**: `TEST_PATHS` is now the glob `userlib/user_devices/*/tests/` — self-updating, all 6 dirs (165 tests) gated; installed to `.git/hooks/pre-push` and verified end-to-end (165 passed in one hook run). The 2026-08-04 claim that one aggregate invocation dies via `double_import_denier` did NOT reproduce (verified twice: explicit 6-dir list + glob form both collect and pass clean) — keep the "don't casually rename `__init__ .py` (space file)" caution, but the per-directory-invocations constraint is retired.
- **BigSky unlaunched-laser warning noise — mitigation menu** (2026-08-05, distribution-analyzed; flood = pre-existing MANUAL-mode poll, ~72/min at idle with YAG_1 off, POST_EXP mask innocent at 6 lines): shared choke point is `_skip_non_success_read` (`RemoteControl/blacs_workers.py:551-576`, serves all 3 GUI devices). Options ranked: **(2, RECOMMENDED) once-per-transition latch** ~18 LOC in the shared helper — WARNING on first/changed `(connection,status,code)`, DEBUG on repeats, SUCCESS clears latch + one INFO "readable again"; volume 3546→~12, cannot mask new faults (any status/code change re-fires; `laser_disconnected` arrives as ERROR ≠ UNKNOWN_CONNECTION), self-heals mid-session, also fixes LaserLock/Rastering's identical latent floods. **(4, follow-up) GUI-declared live list via HELLO** ~35 LOC — HugeSkyController ALREADY ships `connections:[…]` in HELLO (`:88-92`) and BLACS discards it (`RemoteControl/blacs_workers.py:227-236`, zero consumers); gating polls on it removes the wasted REQ-REPs; needs re-HELLO or restart to refresh, glob-matcher test mandatory. **(1b) per-status demotion** ~6 LOC — loses the "forgot to launch YAG_1" cue entirely. **(3) CT dummy/enabled flag (operator's floated idea)** — rated WORST: static (CT edit + recompile + restart to recover), silences `laser_disconnected` for the flagged laser, and `num_lasers=0` collides with the sequence's `YAG_1_voltage=860.0` write. Do NOT ship blanket demotion (1a). **OPERATOR DECISION 2026-08-05: superseded by an expectation model, BigSky-scoped** — reverse tolerant-reads for EXPECTED hardware: an enabled laser that is missing/unreachable goes LOUD (red tab; failed shots — resolves the buffered REJECTED-tolerance item too), and a per-laser "disable" checkbox on the BigSky tab declares a laser intentionally off (no polls, no T2B programming, no warnings). Scope constraint (do not blanket-revert the base): LaserLock un-programmed-setpoint UNKNOWN_CONNECTION at startup (v2 Blocker-A) and rastering uncalibrated typed non-SUCCESS reads MUST stay tolerated. Design workflow in flight → `docs/bigsky-laser-enable-design.md` (DRAFT).
- **Atomic-write keep-vs-revert** (2026-08-05, operator raised): recommendation KEEP — everything observed (25-clamp, px/mm confusion, refused pair) is the pre-existing CT mislabel, reproduced identically by single-axis writes; the compound path is inert for sequences (none set both columns) and improves abort replay. If reverted anyway: `git revert e517c86` on parent is SAFE against the new GUI (per-axis connections retained; unused compound handler is harmless). Decision pending operator.
- ~~**LaserLock queue-liveness hazard**~~ **RESOLVED 2026-08-04** (`open-items-batch` `5fe2e97`+`e5d7fc4`): (a) rastering refusal surfacing needed NO code — the courtesy-write WARNING already lands non-stickily in the tab's OutputBox terminal pane (worker stderr → zprocess `OutputInterceptor` → tab `output_redirection_port`; traced `setup_logging.py:73-79` + `process_tree.py:1424-1433`). `_update_gui_status`/STATUS_TOPICS was a dead end (published by the external GUI process; the worker has no channel onto that socket). Residual gap, deliberate non-fix: terminal pane hidden by default — auto-reveal-on-refusal would be a new worker→tab IPC crossing shared `blacs/` base classes; needs user + blacs-expert sign-off before anyone builds it. (b) LaserLock stays STRICT (user decision); operator note added to `LaserLockDevice/CLAUDE.md`: dismiss a red banner before re-queueing or the queue stalls silently. MERGE NOTE: the branch also edited its own copy of `open-items.md` (`5fe2e97`) — on merge conflict, keep master's version (this file).

## Standing queues (see plans)
- Z1–Z4 ZMQ-v2 cutover set (runbook: `~\.claude\plans\2026-07-07-pr-queue.md`); P4/P6 gated; BigSky refactor branches post-cutover.

## 2026-08-12 — Repo-wide ponytail audit (REPORT COMMITTED, nothing applied — awaiting user review)
- **Full report with per-finding approval checkboxes: `docs/ponytail-audit-2026-08-12.md`** (committed on master). 9 audit passes + 7 domain vets (blacs-expert ×2, lyse-analysis, amo-expert, ablation-tech, BigSky, HF). Net: ~11k lines confirmed cuttable, ~4.3k conditional (each gated), ~1.6k claimed-but-REJECTED — the do-not-cut table is load-bearing (near-misses included `connection_table_open_cell2.py` = the fc810b7 swap-back, `Closed_cell_scan.py` = only scan-wired sequence, BigSky safety guards, HF autoscale/console-fallback).
- **Pickup workflow**: user ticks boxes in the doc → apply Tier 1 in per-cluster commits, honoring the doc's "Process notes before applying" (remove `HF_Locking-zmq-v2` worktree FIRST or every HF edit needs duplicating; backend POST_EXP-path cuts validate with ≥3-shot queue + mid-queue abort; sequences.md-gated deletions must amend that rule in the same commit; pre-push edits need hook re-install per checkout).
- Stale-worktree deletions in the doc (5 merged + 1 empty dir; KEEP `rastering-atomic-xy`, unmerged) subsume the TiSa_1 item's "stale fully-merged branches/worktrees removal candidates pending user OK" above.
- 4 out-of-scope correctness holes found during vetting are listed in the doc for a /code-review pass: BigSky `_remoteSetVoltage` parse-error→SUCCESS, rastering flip checkboxes wired to nothing, NI_DAQmx `manual_mode_task` latent error path (fix as bug + shot test), rastering `ui_path` CWD-relative.

## 2026-08-06 — BigSky strict-comms branch + investigations (ultracode wf_470df03a-5b7; 13 agents, 2 fix rounds)
- **BRANCH READY, MERGE-GATED**: `bigsky-strict-buffered` @ `e7907d5` (worktree `.claude/worktrees/bigsky-strict`, NOT pushed; main checkout untouched). Delivers R1-R7: strict buffered+manual for enabled lasers (any non-SUCCESS or transport None raises), per-YAG Disabled checkbox (persisted, default all-enabled), disabled lasers skipped on writes/polls/auto-arm, arm blanket-except deleted + tri-state verify, 23+ new tests (48 green BigSkyHub+RemoteControl), docs paragraph updated, pre-push TEST_PATHS gap fixed (re-install hook per checkout: `cp .githooks/pre-push .git/hooks/pre-push`).
- **GATE (both final verifiers CONFIRMED, do NOT merge without it or an explicit defer)**: one-line blacs fix — `blacs/blacs/device_base_class.py:676` add `self._final_values = {}` above the `for res in raw_results:` loop. Without it: shot fails (designed R2) -> operator ticks Disabled, clears banner, resumes -> next SUCCESSFUL shot hits `None.update(res)` at `:681` -> RED fatal tab, close disabled, notify_queue never runs -> orphaned-wait queue deadlock. Latent pre-existing blacs bug; strict raising makes it the routine path. Separate repo -> own commit + audit + BLACS restart. Awaiting operator sign-off.
- **Operator briefing before first restart on branch**: offline ENABLED YAG banners the tab at startup (fix = tick Disabled); offline laser groups no longer greyed (clicking banners the tab — R3 contract); all-lasers-disabled => shot h5 has NO monitor_values group (analysis: use .get()); mid-T2B abort can leave earlier-programmed lasers armed (pre-existing abort semantics, now reachable); verify path is zero-retry (one dropped ZMQ reply fails the shot — add 1 retry if spurious failures appear); `_restore_warmup` ignores Disabled (1-line guard if uniformity wanted); get_save_data lacks a test (degrades loud).
- **Arm-path speed hypothesis REFUTED** (operator asked): tolerance born `9a7cc02` 2026-03-06, the day after phantom YAG_2 entered CT (`8724275` num_lasers 1->2; reverted `04c0894` 2026-07-08). Purpose = absent-second-laser resilience. GUI serves CHECK_VALUE from cache (zero serial I/O); verify ~4 ms/laser/shot; poll 6 reads in 3-6 ms. Fix (delete except + tri-state verify) is IN the branch.
- **GUI-close answer**: shot path already raises, but slowly (LaserLock/Rastering T2B = 120 s wait_for_lock timeout; BigSky w/ columns = 5 s; BigSky NO-YAG-columns shot = NEVER raises, ~65 s dead time, runs with unarmed laser). Recommended follow-up: PING pre-flight at top of T2B (~6 LOC base + 3 each BigSky/Rastering) -> 0.5 s loud failure + closes the no-columns hole. Do NOT flip connected=False (stale-True is load-bearing: GUI restart self-heals unattended). Three things operator forgot: clean BigSky close DOES stand hardware down (crash does not); HF lock lives in the WLM app and survives GUI close (argues harder FOR raising); stale-True enables unattended recovery.
- **Rastering units plan (amo-expert CT-side + ablation-tech GUI-side, merged)**: declared mm/(0,25) is 100% unreachable under live calibration — every typeable value maps to NEGATIVE motor mm; real travel 0-12 mm (`GUIs/rastering/config.py:49`); valid pixel window ~112-4417 (x) / 9-3171 (y). LAND NOW (~8 lines, CT-only + recompile + BLACS restart): units="px", limits=(0,4096), decimals=1/step=0.1 on the 4 Raster channels; no GUI change; no out-of-travel risk (GUI motor_bounds is the authority). ACK UX now: (a) readback-match highlight ~10 LOC + (c) "set X / at Y — matched" status text ~15 LOC; explicit Move button (~50-60 LOC, semantics change) defer. mm/(0,12) waits for GUI motor-frame single-axis rows per frame-explicit-coords §(e) ordering.
- **PING pre-flight REJECTED by operator 2026-08-06** ("we dont need new flows") — do NOT re-propose. GUI-death handling stays on existing failure paths: T2B transport-timeout raise (120 s wait_for_lock for LaserLock/Rastering, 5 s BigSky) + arm/verify raises from the strict branch. Residual accepted: discovery latency only; the unarmed-laser silent case is closed by 0a95a7f (arm/verify raise for enabled lasers).
- **Post-apply blacs-expert audit of blacs d1cf0b5: SHIP (2026-08-06)** — placement verified (:680 reset, failure sentinel :683-688 intact, per-shot not per-worker); cross-shot accumulation dependency DISPROVEN: _final_values is consumed at every shot boundary (post_experiment :825 skip_manual branch or T2M :752), and every T2B in the lab returns full sets (NI_DAQmx smart-programming OFF; PrawnBlaster fixed dict) or {} (RemoteControl family, cameras, edge_counter, NI_SCOPE) -> zero queue-end behavior change. Compile OK. Evidence: audit report + 48 userlib tests green on merged master.
- **NEW latent item (pre-existing, surfaced by audit)**: device_base_class.py:825-833 skip_manual branch calls set_value(..., update_gui=True) from the mainloop BACKGROUND thread with neither qtlock nor inmain() (sibling :752 takes qtlock) — Qt thread-safety violation per house rules, runs after EVERY non-final queued shot. Candidate for docs/known-latent-issues.md + fix pass. Also noted: RFBlaster T2M override missing MODE_POST_EXP (fork drift, device unused here).
- **CORRECTION (operator pushback, verified 2026-08-06)**: the :825 side-find is a NON-ISSUE — every set_value in blacs/output_classes.py takes `with qtlock:` INTERNALLY around its widget writes (AO :378, DO :548, DDS :670), so the :825 call-site comment ("no need to grab the QTlock") is literally correct; :752 outer qtlock is redundant, not safer. Same protection on both paths, same mechanism blacs core has used for years. Item retracted — do not schedule a fix.
- **Adversarial re-check of the :825 retraction (2026-08-06)**: verdict = retraction STANDS but reasons corrected. (a) "every set_value locks internally" FALSE — DDS output_classes.py:751 has NO internal qtlock, only transitive via sub-AO/DO; (b) ":752 outer lock redundant" WRONG — qtlock is reentrant, outer lock = one GUI-park for the whole loop (atomic) vs :825 one park PER channel with GUI thread free between (slower, torn-snapshot window); (c) "years without incident" UNSUPPORTED (faulthandler log 0 bytes) — but qtutils qtlock.enforce() profile hook RAISES loudly on any unprotected Qt call from non-main thread, and BLACS.log has zero such hits, so the unprotected-call class is genuinely absent. Mechanism: qtlock parks the GUI thread (no marshalling); house-rule access violations are the thread-AFFINITY class (native window ops), which setValue/blockSignals do not touch. Residual nit (open, not safety): wrap :825-833 in `with qtlock:` to match :752 + fix its contradictory comment — 1 line, atomicity/perf harmonization only.
- **Residual fixes SHIPPED (2026-08-06 ~23:10)**: blacs 81316aa (qtlock once around skip_manual front-panel update — audit SHIP: indentation/reentrancy/no-yield-under-lock all verified) + parent d8aaa83 & 9ee9f18 (_restore_warmup Disabled guard + get_save_data test + mutation-sensitive test fix per audit). 50 tests green. Audit note: guard has NO behavior delta today (sole caller already tab-gated) — pure choke-point defense.
- **Known behavior (pre-existing, operator-facing, decide someday)**: ticking Disabled on an ARMED laser mid-queue clears _is_armed but sends no hardware command — the YAG stays lamps-external/shutter-open with no path restoring warmup (operator sees Disabled checkbox on a still-armed laser). If this bites, the fix is a one-shot stand-down on the disable transition; deliberately NOT added now (a disable tick must stay side-effect-free toward hardware the operator declared absent).
- **h5 verification vs 08/04 reference (2026-08-06 ~23:45)**: post-restart shot saves correctly. Disabled state persisted in front_panel notebook data ({YAG_1: True}); remote_device_operation still records the programmed YAG_1_voltage=860 (provenance of what the sequence asked for while skipped); Raster FP/CT now px. CORRECTION to earlier all-disabled h5 warning: /data/BigSkyLasers and /data/LaserLockGUI monitor_values are absent in PRE-change shots too (base post_experiment writes from _pubsub_cache; BigSky GUI has no PUB-SUB stream at all; LaserLock cache empty on this rig) — absence is pre-existing steady state, NOT a loss from the Disabled feature. BONUS confirmed live: Aug-4 raster frame-tag fix now active (raster attrs typed + @frame=pixel vs string attrs/no frame on 08/04).
- **Disabled-laser greying SHIPPED (79f034c, audit SHIP 2026-08-06 ~23:55)**: every interactive widget in the laser group covered by exactly one disable path; Disabled checkbox never greyed (escape hatch verified — no setEnabled writer touches _disabled_buttons); no fight-back (audit corrected an assumption: _set_ao_widgets_enabled is called ONLY by the Disable Input checkbox, not mode transitions — disabled predicate there is defense, not the main path); GUI-thread only; init order safe. Cosmetic non-blockers: disabled STOP keeps its red QSS (click IS blocked); partial front-panel loads leave unlisted prefixes untouched (pre-existing, same as keep_warm). Activates at next BLACS restart.
- **No-restore Disabled policy SHIPPED (d35ffa5 + polish, audit SHIP 2026-08-07 ~00:20)**: Disabled never restored at startup — only the operator click can disable (never-lie invariant holds by construction: checkbox+grey+worker move together, all writers enumerated); get_save_data still emits the key (shot-h5 provenance); stale disabled_state kwarg inert; every laser-channel comms raise now names the laser + its Disabled checkbox (BigSky _check_response override, exc.args mutation — type/traceback preserved); audit-prescribed polish: prefixes embedded in all arm/warmup/restore contexts so mid-sequence failures carry guidance too. Docs recovery bullet modernized. Activates at next BLACS restart. Audit-gate: audits ran post-edit; final 1-file mechanical polish covered by stated waiver.
