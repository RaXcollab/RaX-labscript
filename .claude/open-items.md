# Open Items — cross-session ledger

Deferred work with an owner and a gate. A Claude session that flags something as "worth a follow-up" MUST add it here (or fix it) — prose footnotes don't survive compaction. Remove items when done.

## Gated on operator / hardware
- **Camera-fix hardware validation** (updated 2026-07-08): morning merges VERIFIED (19:46 15-shot queue clean). Remaining: BLACS restart to activate the overnight hardening (b75b98b + labscript-devices b32c97e) — checklist in `notes/2026-07-08_Nuvu-error27-hardening-and-teardown-fixes.html`; then one slow-lock shot + manual snap.
- **Hooks v2 settings-dispatch test** (plan T2 step 5, narrowed 2026-07-08): scripts smoke-tested 24/24 (both PS editions, byte-exact stdin) — remaining untested surface is Claude Code's settings-level dispatch only. Fresh session → checklist appears (also on `--resume`); scratch edit under `userlib/user_devices/` → audit gate fires once; chained `git add x && git commit` via Bash → blocked by git guard. Note: creating the audit-waiver marker will hit a permission prompt (allow-rule was classifier-denied; waiver now requires user click — keep it that way unless it annoys).

## Code follow-ups (NuvuCamera, pre-existing, audit-flagged 2026-07-08)
- ~~`errorHandling` 107 dead `pass`~~ FIXED in working tree by concurrent session (verified [nc_camera.py:109-116](userlib/user_devices/NuvuCamera/Nuvu_sdk/nc_camera.py#L109-L116) — 107 now returns without closing; 215/216 also reclassified no-close). Committed in `ba5bf63` (verified 2026-08-04: NuvuCamera tree clean, `git log -S` pins both fixes there).
- ~~`get_bias` exposure leak~~ RESOLVED — get_bias/get_bias64 deleted 2026-07-08 (no callers; verified [Nuvu_cam_utils.py:217](userlib/user_devices/NuvuCamera/Nuvu_sdk/Nuvu_cam_utils.py#L217)). Committed in `ba5bf63` (verified 2026-08-04: NuvuCamera tree clean, `git log -S` pins both fixes there).
- ~~Abort-during-continuous double-resume~~ FIXED in b75b98b (`should_resume_continuous` guard + idempotent `start_continuous`; cross-audited, 31/31 tests). All three audit-flagged NuvuCamera items now closed.

## Context follow-ups
- ~~Digest refresh~~ DONE 2026-07-08 (3 opus agents): `cameras-and-scope.md`, `backend-core.md`, `labscriptlib.md` all stamped "Refreshed 2026-07-08" and verified against post-merge code + working tree.
- Always-loaded budget: 11,713 → **7,996 tokens** after 2026-07-08 no-deferrals restructure (under 8k target; history in `.claude/agent-memory/context-auditor/audit-history.md`). Re-measure at next T8 run.
- `Open_cell2.py`: unused `latch_digital` import (post-rework leftover, flagged by digest refresh) — one-line cleanup at next sequence edit.
- **TiSa_1 channel-move merge gate** (added 2026-07-29): TiSa_1 is now WS7 **ch1** (was ch4; crosstalk). The `zmq-v2-cutover` branch still carries `connection=4` in `connection_table.py` (both TiSa_1 children) and double-stale HF docs; `HF_Locking-zmq-v2` has NO per-port tolerance mechanism yet claims one in its CLAUDE.md. Fix at rebase/merge or the move regresses. Full checklist: `docs/wavemeter-channel-move.md`. Physical steps (PID DAC cable output 4→1, WS7 per-channel PID migration) may still be pending — confirm with operator. **Git half CLOSED 2026-08-04:** `zmq-v2-cutover` is fully merged into master (0 unmerged commits, master 14 ahead; TiSa_1 `connection=1` on BOTH branch and master) and HF `zmq-v2-port` is fully merged into HF main (0 unmerged) — no rebase/merge remains that could regress the move. Remaining: operator confirmation of physical steps; stale fully-merged branches/worktrees (`zmq-v2-cutover`, HF `zmq-v2-port`) are removal candidates pending user OK.
- **Graph rebuild gate** (re-armed 2026-07-31): rebuild `graphify-out/graph.json` after the next large cross-root refactor. Recipe + 13-check verify: `.claude/graphify/REFRESH.md`. Last rebuild 2026-07-31 post ZMQ-v2 cutover (commit ee0eff1): 5434n/8835e/403 communities, 34 cross-repo edges incl. the new GUIs→userlib bridge via `external_gui_lib/zmq_v2.py`; 13/13 verify checks passed. Standing convention: re-arm after each rebuild rather than deleting.

- **PR#6 pad-line move** (2026-08-03): committed 04e9012 on `ct-enabled-switchboard` — `PARITY_PAD_LINE` now 6361 `port0/line7`, 6535 `port3/line7` (DIO 31), reserved-line docs in branch CLAUDE.md + `.claude/rules/sequences.md`. Remaining: runtime DAQmx acceptance of the port3 pad rides the PR's existing post-merge gate (BLACS restart + test shot with 6535 on) — remove this line at merge.

- **Rastering edge-coordinate hardening, deferred remainder** (2026-08-04, agent-audited twice; DONE: fresh-partner + wrap `9608d72`, motor_bounds(0-12mm)+units guard+10um edge clamp `6baf4fb`, courtesy-write `f9be922`). **Batch 2026-08-04 (evening): items (2),(3),(5),(6) + (1)-subparts IMPLEMENTED on topic branches `open-items-batch` (parent, 7 commits) + `feat/open-items-batch` (rastering, 6 commits) — adversarially reviewed, suites green (parent 123 per-directory, rastering 120). GATE: merge BOTH repos + coordinated GUI+BLACS restart (user).**
  - (1) **Units pass (user-decided design, refined 2026-08-04):** show BOTH motor and pixel coords, pixel = N/A uncalibrated. **Frame principle: motor units are FIXED (0-12 mm), pixel units vary per calibration re-fit — never tie connection-table bounds to a specific calibration** (a pixel limit like "(0,5000)" is derived from the 2026-07-20 fit; rejected). **Target design: frame-explicit BLACS↔GUI communication — wire supports pixel OR motor coordinates (frame-tagged); once motor-frame comms exist, CT channels can declare motor units with fixed (0,12) limits.** Current CT `(0,25) mm` mislabel is a LABEL defect, **not** a compile blocker (corrected 2026-08-04): all three sequences call `Raster_X.constant(RASTER_X)` and the globals are `RASTER_X = 2.12667` / `RASTER_Y = 1.07629` (h5-verified), inside `(0,25)` *and* the target `(0,12)` — uncommenting compiles today. The `limits`→`LabscriptError` path is real but unarmed; only a pixel-scaled value (e.g. `110`) would trip it. Design doc: `docs/frame-explicit-coords-design.md`. Include in the pass: stamp frame/calibration on h5 `/data/{dev}` monitor_values (uncalibrated mm vs calibrated px currently indistinguishable); one-line latent motor-fallback removals at `raster_controller.py` CHECK_VALUE `:606/:608` + publisher `:2285`; `raster_point_meta` should emit explicit `frame:` key; clamp applies only to calibrated path (asymmetry OK but document). Protocol design gap: v2 HELLO has no per-connection units/frame declaration — the natural home for the frame tag. **PROGRESS 2026-08-04:** subparts landed on `feat/open-items-batch` — motor-fallbacks removed (`:606/:608`, `:2285`), `raster_point_meta` emits `frame:`, dual-frame readout mm-always/px-when-calibrated (`ef7c0b8`); parent `05a3acd` adds `frame` to `RASTER_META_KEYS` so it reaches shot h5 (review blocker, fixed). Design doc DRAFTED, uncommitted, awaiting user review: `docs/frame-explicit-coords-design.md` (261 lines, adversarially reviewed + revised against 12 verified findings; 'Open questions for the user' at end — incl. frame-token naming pixel-vs-target, invented `calibration_id` mechanism, RASTER_X/Y globals must become mm at migration). Remaining: monitor_values frame/calibration stamping is design-gated (PUB-SUB cache carries no frame today — doc §d); BLACS-side `frame` passthrough.
  - (2) **Atomic (x,y) write — verified CONTAINED, not a refactor** (blacs-expert 2026-08-04): ~30-40 LOC, 2 files. Worker-level interception: `RasteringWorker.program_manual`/`transition_to_buffered` already hold both values (panel dict / one h5 row) → one compound PROGRAM_VALUE (e.g. `laser_raster_xy`) → GUI executes with EXISTING `request_move_target`/MOVE_TARGET (already clamps + bounds-checks the true pair). No CT/tab/widget/PUB/read-side changes. Gotchas: one-column h5 table (sequence sets only x) keeps single-axis path — NEVER fill partner from stale panel; compound failure must route through `_on_program_manual_error` or courtesy-write policy silently dies. Bonus: deletes the physical `(x_new, y_old)` intermediate excursion (only failure mode the clamp can't fix) + halves abort-path round trips. **IMPLEMENTED 2026-08-04**: parent `e517c86` (worker interception, gotchas honored, 6 new tests) + rastering `202004e` (compound handler w/ optional `frame` arg: `pixel` default, `motor`→MOVE_MOTOR, typed `invalid_frame`/`invalid_value`). Cross-side wire contract adversarially verified. **DEPLOY COUPLING: merge both + restart GUI and BLACS together — new BLACS against old GUI fails every both-axis shot with `unknown_connection` (manual writes only warn).** Residual notes (review + blacs-expert audit 2026-08-05, verdict SHIP-WITH-NOTES): **`program_value` args passthrough re-scoped from cosmetic to SHOT-FAILURE risk** — BLACS can't send `timeout_sec` (client hardcodes `args={"wait_for_lock"}`), so the GUI's internal 10 s cap binds while MOVE_TARGET moves x THEN y sequentially against ONE reply budget; a pair move with `10 s < t_x+t_y ≤ 20 s` in T2B → `motor_move_failed` → strict raise → **shot fails, sticky error, queue stalls** (bites a large jump, e.g. first shot from a parked position; typical raster steps are sub-second; `program_manual` side is harmless — courtesy warn while the motion completes). Fix = args-passthrough kwarg on the shared `program_value` (same change unlocks the frame tag / motor-frame writes — see design doc). Also: `RasteringWorker.program_manual` override duplicates the base loop verbatim (mirror future base-loop changes); compound connection-name literal not test-pinned across repos.
  - (3) **`MODE_POST_EXP` poll mask** — verified: blind window is inter-shot bookkeeping only (poll runs through MODE_BUFFERED; out-of-mode tick DROPPED not deferred, timer re-arms → worst case one missed 5s tick). Fix = add `|MODE_POST_EXP` to `RemoteControl/blacs_tabs.py:363-366` mask (fork's own `device_base_class.py:456` base already includes it — omission reads as oversight). Cost: REQ-REPs in inter-shot gap; hung GUI could delay next T2B ≤5s. UNBOUNDED exception: device in shot N but absent from later queued shots strands in POST_EXP (poll + program_device frozen) until pause — latent, rastering is in every shot. Fresher echo ≠ safer abort replay; decide on merits. **IMPLEMENTED 2026-08-04** `d66c782` on `open-items-batch` (tradeoff documented in commit body). Audit corrections (blacs-expert 2026-08-05): hung-GUI worst case is **N×5 s per device**, not ≤5 s — the poll loops every output channel and short-circuits only on a hard `None` (Rastering 2→10 s, LaserLock 3→15 s, BigSky 9→45 s; queue `timeout_limit=300` so the gap stretches, queue survives). Drop the commit at merge if that exposure is unacceptable. Stranded-tab escape is PARTIAL: the mask un-freezes the echo only — `program_device` stays MODE_MANUAL-only, operator edits still queue until pause. `status_monitor` (blacs_tabs.py:392-402) is DEAD CODE (zero call sites, audit-verified) — its old mask is moot. Pre-existing noise, newly extended into the gap: BigSky's 3 non-checkable params each log a `_skip_non_success_read` WARNING per poll. Runtime verify at next BLACS restart: queue 3+ shots, compare PERF T2B timestamps.
  - (4) Clamp ceiling: `MOTOR_EDGE_CLAMP_MM=0.010` sized to current cal cross-terms (≤~5um full-field); a re-fit with larger cross-terms re-opens edge rejections — re-check after recalibration.
  - ~~(5) BLACS-tab `raster_progress` shows e.g. "37/12" after wrap~~ FIXED 2026-08-04 on `feat/open-items-batch` (`5fede41`+`d84a735`): `_raster_step_count` field deleted (a reset-on-wrap would race the queued Qt increment slot); progress text now derived wrap-sane at publish time.
  - (6) Dead code: ~~`request_jog_target`~~ deleted `e538aa0` (no callers, repo-wide grep); BigSky cosmetic substrings converted to typed checks in `75a41a3`. Leftover: orphaned `CommandType.JOG_TARGET` enum + `_execute` branch (only a test constructs it directly) — remove at next controller edit.

- **BigSky buffered-path typed conversion** (2026-08-04, from the program_manual consolidation audit — the manual path is DONE, hook + typed gates, 60/60 tests): `should_skip_buffered_response` (`BigSkyHub/blacs_workers.py:42`) still relies on `_SKIP_ERROR_SUBSTRINGS` — **load-bearing**: an offline laser arrives as `status=ERROR` + `code=laser_disconnected` and only the "laser disconnected" substring catches it (guard comment added at the tuple). Convert to typed `error.code` checks (add `laser_disconnected`), then retire the substrings. Also cosmetic-only substring sites at `_arm_laser` (~:189, greps `str(exc)`) and `_verify_armed_state` (~:547) — log-level only, both branches act identically. **DONE 2026-08-04** `75a41a3` on `open-items-batch`: buffered path on typed `error.code` gates (each substring mapped to its typed carrier and verified against HugeSkyController.pyw emissions BEFORE retiring; cosmetic sites converted; 19/19 tests incl. skip-on-`laser_disconnected` and raise-on-unknown-code). Review: pass-with-notes — two notes promoted to items below.
- **BigSky buffered REJECTED-any-code tolerance** (pre-existing, surfaced by 2026-08-04 review — entrenched, not introduced, by `75a41a3`): the buffered path skips on `status=REJECTED` regardless of `error.code`, so an h5-programmed refusal (e.g. `voltage_out_of_range`, interlock) silently skips instead of failing the shot (`BigSkyHub/blacs_workers.py:46`, skip at `:490-494` precedes `_check_response`). Decide: narrow to specific codes, or document as intended queue-liveness behavior.
- **Pre-push hook TEST_PATHS gap** (2026-08-04 review): `.githooks/pre-push:47` runs only NuvuCamera + NI_SCOPE — BigSkyHub/RasteringDevice/RemoteControl suites (74 tests) never run pre-push. Constraint: one aggregate pytest invocation across device dirs dies in collection via `double_import_denier` (pre-existing; root involves the TRACKED oddly-named `userlib/user_devices/__init__ .py` — filename contains a space → namespace package → dual import spellings). Fix = per-directory invocations in the hook; do NOT casually rename the space file (BLACS runtime import semantics).
- ~~**LaserLock queue-liveness hazard**~~ **RESOLVED 2026-08-04** (`open-items-batch` `5fe2e97`+`e5d7fc4`): (a) rastering refusal surfacing needed NO code — the courtesy-write WARNING already lands non-stickily in the tab's OutputBox terminal pane (worker stderr → zprocess `OutputInterceptor` → tab `output_redirection_port`; traced `setup_logging.py:73-79` + `process_tree.py:1424-1433`). `_update_gui_status`/STATUS_TOPICS was a dead end (published by the external GUI process; the worker has no channel onto that socket). Residual gap, deliberate non-fix: terminal pane hidden by default — auto-reveal-on-refusal would be a new worker→tab IPC crossing shared `blacs/` base classes; needs user + blacs-expert sign-off before anyone builds it. (b) LaserLock stays STRICT (user decision); operator note added to `LaserLockDevice/CLAUDE.md`: dismiss a red banner before re-queueing or the queue stalls silently. MERGE NOTE: the branch also edited its own copy of `open-items.md` (`5fe2e97`) — on merge conflict, keep master's version (this file).

## Standing queues (see plans)
- Z1–Z4 ZMQ-v2 cutover set (runbook: `~\.claude\plans\2026-07-07-pr-queue.md`); P4/P6 gated; BigSky refactor branches post-cutover.
