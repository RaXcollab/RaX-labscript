# Open Items — cross-session ledger

Deferred work with an owner and a gate. A Claude session that flags something as "worth a follow-up" MUST add it here (or fix it) — prose footnotes don't survive compaction. Remove items when done.

## Gated on operator / hardware
- **Camera-fix hardware validation** (updated 2026-07-08): morning merges VERIFIED (19:46 15-shot queue clean). Remaining: BLACS restart to activate the overnight hardening (b75b98b + labscript-devices b32c97e) — checklist in `notes/2026-07-08_Nuvu-error27-hardening-and-teardown-fixes.html`; then one slow-lock shot + manual snap.
- **Hooks v2 settings-dispatch test** (plan T2 step 5, narrowed 2026-07-08): scripts smoke-tested 24/24 (both PS editions, byte-exact stdin) — remaining untested surface is Claude Code's settings-level dispatch only. Fresh session → checklist appears (also on `--resume`); scratch edit under `userlib/user_devices/` → audit gate fires once; chained `git add x && git commit` via Bash → blocked by git guard. Note: creating the audit-waiver marker will hit a permission prompt (allow-rule was classifier-denied; waiver now requires user click — keep it that way unless it annoys).

## Code follow-ups (NuvuCamera, pre-existing, audit-flagged 2026-07-08)
- ~~`errorHandling` 107 dead `pass`~~ FIXED in working tree by concurrent session (verified [nc_camera.py:109-116](userlib/user_devices/NuvuCamera/Nuvu_sdk/nc_camera.py#L109-L116) — 107 now returns without closing; 215/216 also reclassified no-close). Pending commit.
- ~~`get_bias` exposure leak~~ RESOLVED — get_bias/get_bias64 deleted 2026-07-08 (no callers; verified [Nuvu_cam_utils.py:217](userlib/user_devices/NuvuCamera/Nuvu_sdk/Nuvu_cam_utils.py#L217)). Pending commit.
- ~~Abort-during-continuous double-resume~~ FIXED in b75b98b (`should_resume_continuous` guard + idempotent `start_continuous`; cross-audited, 31/31 tests). All three audit-flagged NuvuCamera items now closed.

## Context follow-ups
- ~~Digest refresh~~ DONE 2026-07-08 (3 opus agents): `cameras-and-scope.md`, `backend-core.md`, `labscriptlib.md` all stamped "Refreshed 2026-07-08" and verified against post-merge code + working tree.
- Always-loaded budget: 11,713 → **7,996 tokens** after 2026-07-08 no-deferrals restructure (under 8k target; history in `.claude/agent-memory/context-auditor/audit-history.md`). Re-measure at next T8 run.
- `Open_cell2.py`: unused `latch_digital` import (post-rework leftover, flagged by digest refresh) — one-line cleanup at next sequence edit.
- **TiSa_1 channel-move merge gate** (added 2026-07-29): TiSa_1 is now WS7 **ch1** (was ch4; crosstalk). The `zmq-v2-cutover` branch still carries `connection=4` in `connection_table.py` (both TiSa_1 children) and double-stale HF docs; `HF_Locking-zmq-v2` has NO per-port tolerance mechanism yet claims one in its CLAUDE.md. Fix at rebase/merge or the move regresses. Full checklist: `docs/wavemeter-channel-move.md`. Physical steps (PID DAC cable output 4→1, WS7 per-channel PID migration) may still be pending — confirm with operator.
- **Graph rebuild gate** (re-armed 2026-07-31): rebuild `graphify-out/graph.json` after the next large cross-root refactor. Recipe + 13-check verify: `.claude/graphify/REFRESH.md`. Last rebuild 2026-07-31 post ZMQ-v2 cutover (commit ee0eff1): 5434n/8835e/403 communities, 34 cross-repo edges incl. the new GUIs→userlib bridge via `external_gui_lib/zmq_v2.py`; 13/13 verify checks passed. Standing convention: re-arm after each rebuild rather than deleting.

- **PR#6 pad-line move** (2026-08-03): committed 04e9012 on `ct-enabled-switchboard` — `PARITY_PAD_LINE` now 6361 `port0/line7`, 6535 `port3/line7` (DIO 31), reserved-line docs in branch CLAUDE.md + `.claude/rules/sequences.md`. Remaining: runtime DAQmx acceptance of the port3 pad rides the PR's existing post-merge gate (BLACS restart + test shot with 6535 on) — remove this line at merge.

- **Rastering edge-coordinate hardening, deferred remainder** (2026-08-04, agent-audited; fixes for the incident class landed in `fix/fresh-partner-coord` + parent `RasteringWorker.program_manual` override): (1) connection-table `limits=(0, 25.0) mm` on `laser_raster_*_coord` is target-units-dishonest — spinbox display clamps at 25 while the real value is ~110-200; pick the real target range with the operator. (2) `motor_bounds = None` in `GUIs/rastering/config.py` — setting real Z912 travel would turn Kinesis .NET exceptions into typed "motor out of bounds" rejections (verify travel = 12 mm before setting). (3) Optional epsilon-clamp at the travel floor so replaying a measured edge position succeeds instead of warning. (4) Torn (x,y) echo pairs: `check_remote_values` polls channels in separate REQ-REPs; a raster step between replies fabricates a front-panel pair that was never a real point. (5) `check_remote_values` allowed-modes omit `MODE_POST_EXP` — front panel stale during queues. (6) BLACS-tab `raster_progress` shows e.g. "37/12" after wrap (`_raster_step_count` never resets per pass). (7) x-before-y programming order rests on alphabetical sort of `Raster_X`/`Raster_Y` labscript names — nothing asserts it.

- **BigSky buffered-path typed conversion** (2026-08-04, from the program_manual consolidation audit — the manual path is DONE, hook + typed gates, 60/60 tests): `should_skip_buffered_response` (`BigSkyHub/blacs_workers.py:42`) still relies on `_SKIP_ERROR_SUBSTRINGS` — **load-bearing**: an offline laser arrives as `status=ERROR` + `code=laser_disconnected` and only the "laser disconnected" substring catches it (guard comment added at the tuple). Convert to typed `error.code` checks (add `laser_disconnected`), then retire the substrings. Also cosmetic-only substring sites at `_arm_laser` (~:189, greps `str(exc)`) and `_verify_armed_state` (~:547) — log-level only, both branches act identically.
- ~~**LaserLock queue-liveness hazard, LaserLock half**~~ (2026-08-04 audit, latent) CLOSED 2026-08-04: LaserLock keeps the STRICT base policy by decision (no courtesy-write override) — documented instead: `userlib/user_devices/LaserLockDevice/CLAUDE.md` "Operator note" — dismiss a red LaserLock/HF error banner before re-queueing shots, or the queue stalls silently (sticky `tab.error_message` + `experiment_queue.py:495-501` refuses `transition_device_to_buffered` while it's set).
- ~~**LaserLock queue-liveness hazard, rastering half**~~ (2026-08-04 audit) CLOSED 2026-08-04, no code change — verified ALREADY COVERED: `RasteringWorker._on_program_manual_error`'s `self.logger.warning(...)` already reaches the tab's terminal pane non-stickily. Trace: worker stdout/stderr is redirected to the tab's `OutputBox` at worker creation (`blacs/blacs/tab_base_classes.py:520` `output_redirection_port=self._output_box.port`, wired before `setup_logging()` runs in the subprocess per `zprocess/process_tree.py:1424-1433`), and `labscript_utils/setup_logging.py:73-79` streams WARNING+ to stderr — which the OutputBox displays (red). The `_update_gui_status`/STATUS_TOPICS candidate considered and rejected: those PUB-SUB topics are published by the EXTERNAL rastering GUI process, not the BLACS worker — the worker has no channel onto that socket. Operator still has to click "Show Terminal" to see it; auto-revealing it would need a new worker→tab IPC channel (touches shared `device_base_class.py`/`tab_base_classes.py`) — out of budget for a courtesy fix, not attempted.

## Standing queues (see plans)
- Z1–Z4 ZMQ-v2 cutover set (runbook: `~\.claude\plans\2026-07-07-pr-queue.md`); P4/P6 gated; BigSky refactor branches post-cutover.
