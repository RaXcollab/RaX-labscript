"""Shared library for external GUI hubs that talk to BLACS via ZMQ.

Currently exposes the v2 RemoteControl protocol foundation
(`zmq_v2` module). Future modules can land here as cross-GUI
helpers are extracted.

See ``docs/remotecontrol-zmq-protocol-v2.md`` for the v2 protocol spec
(canonical reference).
"""
