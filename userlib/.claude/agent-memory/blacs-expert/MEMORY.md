# blacs-expert memory

- [v2 message alias vs substring gates](reference_v2-message-alias-substring-gates.md) — `response["message"]` still carries `error.message`; "the v1 substring gates are dead" is false, and one of them is load-bearing for buffered shots.
